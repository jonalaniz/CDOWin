<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.RemindersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CDOWin.ViewModels" 
    xmlns:local1="using:CDO.Core.Models"
    d:DataContext="{d:DesignInstance Type=viewmodels:RemindersViewModel}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="316"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0"
                    Grid.Column="0">
            <TextBlock Text="Reminders"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="16,0,16,1"/>
            <TextBlock FontSize="12"
                       Margin="16,1,16,8"
                       FontWeight="Light">
                    <Run Text="{x:Bind ViewModel.Reminders.Count, Mode=OneWay}"/>
                    <Run Text="Reminders"/>
            </TextBlock>
        </StackPanel>

        <CalendarView Grid.Row="1"
                      Grid.Column="0"
                      Margin="0,8,0,8"
                      HorizontalAlignment="Center"
                      x:Name="RemindersCalendar"
                      CalendarViewDayItemChanging="RemindersCalendar_CalendarViewDayItemChanging"
                      SelectionMode="Single"
                      IsGroupLabelVisible="True"
                      IsOutOfScopeEnabled="True"
                      Language="en"
                      CalendarIdentifier="GregorianCalendar"/>

        <ScrollViewer Grid.Row="2"
                      Grid.Column="0">
            <ItemsRepeater ItemsSource="{Binding Reminders}"
                           HorizontalAlignment="Stretch">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local1:Reminder">
                        <Border Background="{ThemeResource SubtleFillColorSecondary}"
                                CornerRadius="8"
                                Margin="8,6,8,6"
                                Padding="14,12,14,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0"
                                      Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{x:Bind clientName}"
                                               FontSize="14"
                                               FontWeight="Bold" />
                                    <TextBlock Grid.Column="1"
                                               Text="{x:Bind localDate}"
                                               FontSize="14"
                                               FontWeight="Normal"/>
                                </Grid>

                                <TextBlock Grid.Row="1" 
                                           Text="{x:Bind description}"
                                           TextWrapping="WrapWholeWords"
                                           FontWeight="Light"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
