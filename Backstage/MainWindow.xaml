<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Backstage.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Backstage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Backstage">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <TitleBar Grid.Row="0" x:Name="Tbar" Height="48"
                  Title="Backstage" Subtitle="Beta"
                  IsBackButtonVisible="False" IsPaneToggleButtonVisible="True"
                  PaneToggleRequested="PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square44x44Logo.scale-200.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView Grid.Row="1" x:Name="MainNavigationView"
                        IsBackButtonVisible="Collapsed" 
                        IsPaneToggleButtonVisible="False"
                        SelectionChanged="nvSample_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="ContactInfo" Content="Clients" Tag="SamplePage1" />
                <NavigationViewItem Icon="People" Content="Users" Tag="SamplePage2" />
            </NavigationView.MenuItems>
            <Frame x:Name="contentFrame"/>
        </NavigationView>
    </Grid>
</Window>
