<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Clients.ClientViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sas="using:CDO.Core.DTOs.SAs"
    xmlns:placements="using:CDO.Core.DTOs.Placements"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:dialogs="using:CDOWin.Views.Clients.Dialogs"
    xmlns:c="using:CDO.UI.Shared.Controls"
    mc:Ignorable="d">

    <Grid BorderThickness="0, 0, 1, 0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
          Visibility="{x:Bind _viewModel.Selected, Mode=OneWay, Converter={StaticResource NullToVisability}}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Row 0: Title + Buttons -->
        <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource PageTitleStyle}"
                   Opacity="{x:Bind _viewModel.Selected.InActive, Mode=OneWay, Converter={StaticResource BoolOpacityConverter}}"
                   Text="{x:Bind _viewModel.Selected.FormattedName, Mode=OneWay}"
                   x:Name="Header" RightTapped="TextBlock_RightTapped"/>

        <!-- CommandBar -->
        <CommandBar Grid.Row="0" Grid.Column="1" 
                    Style="{StaticResource CommandBarDefaultStyle}">
            
            <!-- Documents Button -->
            <AppBarButton Width="56" Label="Documents" Click="OpenDocuments_Clicked" IsCompact="False" ToolTipService.ToolTip="Open Documents">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE838;" Style="{StaticResource SingleIconFontStyle}"/>
                </AppBarButton.Icon>
            </AppBarButton>
            
            <!-- Reminders Button -->
            <AppBarButton Width="56" Label="New Reminder" Click="CreateReminder_ClickAsync" IsCompact="False" ToolTipService.ToolTip="New Reminder">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xED0E;" Style="{StaticResource SingleIconFontStyle}"/>
                </AppBarButton.Icon>
            </AppBarButton>
            
            <!-- Menu Button -->
            <AppBarButton Width="56" IsCompact="False" ToolTipService.ToolTip="More">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE712;" Style="{StaticResource SingleIconFontStyle}"/>
                </AppBarButton.Icon>
                <AppBarButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                        <MenuFlyoutItem Text="Edit..." Click="EditButton_Clicked" Tag="{x:Bind dialogs:ClientEditType.Administrative}">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Edit" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="{x:Bind _viewModel.Selected.Active, Mode=OneWay, Converter={StaticResource BoolActiveConverter}}"
                                        Click="ToggleActive_Clicked"/>
                        <MenuFlyoutItem Text="{x:Bind _viewModel.Selected.TTW, Mode=OneWay, Converter={StaticResource BoolTTWConverter}}"
                                        Click="ToggleTTW_Clicked"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Delete..."
                                        Click="Delete_Clicked">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Style="{StaticResource DeleteSymbolIcon}"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>

        <!-- Row 1: ScrollViewer + WrapPanel -->
        <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
            
            <!-- Wrap Panel -->
            <controls:WrapPanel Orientation="Horizontal" Margin="8,0,8,8" HorizontalSpacing="8" VerticalSpacing="8">

                <!-- Personal Information Card-->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Style="{StaticResource CDOCardHeaderContainerStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                            
                        <TextBlock Grid.Column="0" Text="Personal Information"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}"
                                Tag="{x:Bind dialogs:ClientEditType.Personal}" Click="EditButton_Clicked"
                                ToolTipService.ToolTip="Edit">
                            <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        </Button>
                    </Grid>
                        
                    <!-- Content -->
                    <ScrollView Grid.Row="1">
                        <Grid Style="{StaticResource CDOCardContentContainerStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Date of Birth/DL/SSN -->
                            <c:LabelPair Grid.Row ="0" Grid.Column="0" Grid.ColumnSpan="2" Label="DOB"
                                     Value="{x:Bind _viewModel.Selected.FormattedDOB, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Label="DL/ID"
                                     Value="{x:Bind _viewModel.Selected.DriversLicense, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="0" Grid.Column="4" Grid.ColumnSpan="2" Label="SSN"
                                     Value="{x:Bind _viewModel.Selected.FormattedSSN, Mode=OneWay}"/>

                            <!-- Fluent Languages/Race -->
                            <c:LabelPair Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Label="Fluent Languages"
                                     Value="{x:Bind _viewModel.Selected.FluentLanguages, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="1" Grid.Column="3" Grid.ColumnSpan="3" Label="Race"
                                     Value="{x:Bind _viewModel.Selected.Race, Mode=OneWay}"/>

                            <!-- Address Section -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="6"  
                                    Visibility="{x:Bind _viewModel.Selected.FormattedAddress, Mode=OneWay, Converter={StaticResource StringToVisability}}">
                                <TextBlock Style="{StaticResource LabelTextBlockStyle}" Text="Address"/>
                                <TextBlock Style="{StaticResource ValueTextBlockStyle}"
                                       Text="{x:Bind _viewModel.Selected.FormattedStreetAddress, Mode=OneWay}"/>
                                <TextBlock Style="{StaticResource ValueTextBlockStyle}"
                                       Text="{x:Bind _viewModel.Selected.FormattedCityStateZip, Mode=OneWay}"/>
                            </StackPanel>

                            <!-- Education Section -->
                            <c:LabelPair Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="6" Label="Education"
                                     Value="{x:Bind _viewModel.Selected.Education, Mode=OneWay}"/>
                        </Grid>
                    </ScrollView>
                </Grid>

                <!-- Contact Information Card -->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Style="{StaticResource CDOCardHeaderContainerStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Contact Information" Style="{StaticResource SubtitleTextBlockStyle}"/>

                        <!-- Edit Contact Information Button -->
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}" 
                                Tag="{x:Bind dialogs:ClientEditType.Contact}" Click="EditButton_Clicked"
                                ToolTipService.ToolTip="Edit">
                            <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        </Button>
                    </Grid>
                    
                    <!-- Content -->
                    <Grid Grid.Row="1" Style="{StaticResource CDOCardContentContainerStyle}" RowSpacing="8" ColumnSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Phone 1 -->
                        <FontIcon Grid.Row="0" Grid.Column="0" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}" Glyph="&#xE717;"
                                  Visibility="{x:Bind _viewModel.Selected.Phone1, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>
                        <c:LabelPair Grid.Row="0" Grid.Column="1" 
                                     Label="{x:Bind _viewModel.Selected.Phone1Identity, Mode=OneWay}" 
                                     Value="{x:Bind _viewModel.Selected.Phone1, Mode=OneWay}" 
                                     Visibility="{x:Bind _viewModel.Selected.Phone1, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>

                        <!-- Phone 2 -->
                        <c:LabelPair Grid.Row="1" Grid.Column="1"
                                     Label="{x:Bind _viewModel.Selected.Phone2Identity, Mode=OneWay}" 
                                     Value="{x:Bind _viewModel.Selected.Phone2, Mode=OneWay}"
                                     Visibility="{x:Bind _viewModel.Selected.Phone2, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>

                        <!-- Phone 3 -->
                        <c:LabelPair Grid.Row="2" Grid.Column="1"
                                    Label="{x:Bind _viewModel.Selected.Phone3Identity, Mode=OneWay}" 
                                    Value="{x:Bind _viewModel.Selected.Phone3, Mode=OneWay}"
                                    Visibility="{x:Bind _viewModel.Selected.Phone3, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>

                        <!-- Email 1 -->
                        <FontIcon Grid.Row="3" Grid.Column="0" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}" Glyph="&#xE715;" 
                                Visibility="{x:Bind _viewModel.Selected.Email, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>

                        <c:LabelPair Grid.Row="3" Grid.Column="1"
                                    Label="{x:Bind _viewModel.Selected.EmailIdentity, Mode=OneWay}" 
                                    Value="{x:Bind _viewModel.Selected.Email, Mode=OneWay}"
                                    Visibility="{x:Bind _viewModel.Selected.Email, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>

                        <!-- Email 2 -->
                        <c:LabelPair Grid.Row="4" Grid.Column="1"
                                     Label="{x:Bind _viewModel.Selected.Email2Identity, Mode=OneWay}" 
                                     Value="{x:Bind _viewModel.Selected.Email2, Mode=OneWay}"
                                     Visibility="{x:Bind _viewModel.Selected.Email2, Mode=OneWay, Converter={StaticResource StringToVisability}}"/>
                    </Grid>
                </Grid>

                <!-- Case Information Card -->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Style="{StaticResource CDOCardHeaderContainerStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Case Information" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}" ToolTipService.ToolTip="Edit"
                                    Tag="{x:Bind dialogs:ClientEditType.Case}" Click="EditButton_Clicked">
                            <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        </Button>
                    </Grid>

                    <!-- Content -->
                    <ScrollView Grid.Row="1">
                        <Grid Style="{StaticResource CDOCardContentContainerStyle}" ColumnSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Start Date/Case ID/Tier Section -->
                            <c:LabelPair Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Label="Start Date" 
                                     Value="{x:Bind _viewModel.Selected.FormattedStartDate, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" Label="Case ID" 
                                     Value="{x:Bind _viewModel.Selected.CaseID, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="0" Grid.Column="4" Grid.ColumnSpan="2" Label="Case Load"
                                     Value="{x:Bind _viewModel.Selected.CounselorReference.CaseLoadID, Mode=OneWay}"/>

                            <!-- Benefits/Premiums Section -->
                            <c:LabelPair Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Label="Benefits" 
                                     Value="{x:Bind _viewModel.Selected.Benefits, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Label="Premiums" 
                                     Value="{x:Bind _viewModel.Selected.Premiums, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2" Label="Tier" 
                                     Value="{x:Bind _viewModel.Selected.Status, Mode=OneWay}"/>

                            <!-- Counselor Section -->
                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="6"
                                        Visibility="{x:Bind _viewModel.Selected.CounselorReference, Mode=OneWay, Converter={StaticResource StringToVisability}}">
                                <TextBlock Style="{StaticResource LabelTextBlockStyle}" Text="Counselor"/>

                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="1" VerticalAlignment="Top" Margin="0,6,12,0"
                                            Style="{StaticResource AccentButtonStyle}"
                                            IsEnabled="{x:Bind _viewModel.Selected.CounselorID, Mode=OneWay, Converter={StaticResource NullToBoolConverter}}"
                                            Tag="{x:Bind _viewModel.Selected.CounselorID, Mode=OneWay}" Click="GoToCounselor_Click">
                                        <FontIcon Glyph="&#xE76C;"/>
                                    </Button>

                                    <StackPanel Grid.Column="0" Orientation="Vertical">
                                        <TextBlock Style="{StaticResource ValueTextBlockStyle}"
                                                   Text="{x:Bind _viewModel.Selected.CounselorReference.Name, Mode=OneWay}"/>
                                        <TextBlock Style="{StaticResource ValueTextBlockStyle}"
                                                   Text="{x:Bind _viewModel.Selected.CounselorReference.Phone, Mode=OneWay}"/>
                                        <TextBlock TextWrapping="Wrap" Style="{StaticResource ValueTextBlockStyle}"
                                                   Text="{x:Bind _viewModel.Selected.CounselorReference.Email, Mode=OneWay}"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                            
                            

                                <!-- Secretary Section -->
                            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="6"
                                    Visibility="{x:Bind _viewModel.Selected.CounselorReference, Mode=OneWay, Converter={StaticResource StringToVisability}}">
                                <TextBlock Style="{StaticResource LabelTextBlockStyle}" Text="Secretary"/>
                                <TextBlock Style="{StaticResource ValueTextBlockStyle}"
                                       Text="{x:Bind _viewModel.Selected.CounselorReference.SecretaryName, Mode=OneWay}"/>
                                <TextBlock Style="{StaticResource ValueTextBlockStyle}"
                                       Text="{x:Bind _viewModel.Selected.CounselorReference.SecretaryEmail, Mode=OneWay}"/>
                            </StackPanel>
                        </Grid>
                    </ScrollView>
                </Grid>

                <!-- Conditions Card -->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Grid Grid.Row="0" Style="{StaticResource CDOCardHeaderContainerStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Conditions"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}"
                                Tag="{x:Bind dialogs:ClientEditType.Conditions}" Click="EditButton_Clicked"
                                ToolTipService.ToolTip="Edit">
                            <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        </Button>
                    </Grid>

                    <!-- Content -->
                    <ScrollView Grid.Row="1">
                        <Grid Grid.Row="1" Style="{StaticResource CDOCardContentContainerStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Start Date/Case ID/Status Section -->
                            <c:LabelPair Grid.Row="0" Grid.Column="0" Label="Employment Goals"
                                     Value="{x:Bind _viewModel.Selected.EmploymentGoal, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="1" Grid.Column="0" Label="Conditions"
                                      Value="{x:Bind _viewModel.Selected.Conditions, Mode=OneWay}"/>
                        </Grid>
                    </ScrollView>
                </Grid>

                <!-- Service Authorizations Card -->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="6" Margin="12,6,6,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Service Authorizations" Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}"
                                Click="CreateSA_Click" ToolTipService.ToolTip="New Service Authorization">
                            <FontIcon Glyph="&#xE710;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        </Button>
                    </Grid>

                    <!-- Item Repeater -->
                    <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                        <ItemsRepeater ItemsSource="{x:Bind _viewModel.Invoices, Mode=OneWay}">
                            <DataTemplate x:DataType="sas:SADetail">

                                <!-- Button -->
                                <Button Style="{StaticResource BackstageListButton}" 
                                        Click="SA_Click" Tag="{x:Bind Id}">
                                    <StackPanel Spacing="2">
                                        <TextBlock FontWeight="Bold" Text="{x:Bind ServiceAuthorizationNumber}"/>
                                        <TextBlock Text="{x:Bind Description}" TextWrapping="WrapWholeWords"/>
                                        <TextBlock FontWeight="SemiBold" Text="{x:Bind FormattedDateRange}"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsRepeater>
                    </ScrollViewer>
                </Grid>
                
                <!-- Employment Profile Card -->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Style="{StaticResource CDOCardHeaderContainerStyle}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Employment Profile"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}"
                                    Tag="{x:Bind dialogs:ClientEditType.Employment}" Click="EditButton_Clicked"
                                    ToolTipService.ToolTip="Edit">
                            <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                        </Button>
                    </Grid>

                    <!-- Content -->
                    <ScrollView Grid.Row="1">
                        <Grid Style="{StaticResource CDOCardContentContainerStyle}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Start Date/Case ID/Status Section -->
                            <c:LabelPair Grid.Row="0" Grid.Column="0" Label="Disability" 
                                     Value="{x:Bind _viewModel.Selected.Disability, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="1" Grid.Column="0" Label="Criminal Charges" 
                                     Value="{x:Bind _viewModel.Selected.CriminalCharge, Mode=OneWay}"/>

                            <c:LabelPair Grid.Row="2" Grid.Column="0" Label="Transportation" 
                                     Value="{x:Bind _viewModel.Selected.Transportation, Mode=OneWay}"/>
                        </Grid>
                    </ScrollView>
                </Grid>

                <!-- Placements Card -->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                        <!-- Header -->
                    <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="6" Margin="12,6,6,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Placements"
                                    Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}"
                                Click="CreatePlacement_Click">
                            <FontIcon Glyph="&#xE710;" FontSize="16" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"
                                        ToolTipService.ToolTip="New Placement"/>
                        </Button>
                    </Grid>

                        <!-- Item Repeater -->
                    <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                        <ItemsRepeater ItemsSource="{x:Bind _viewModel.Placements, Mode=OneWay}">
                            <DataTemplate x:DataType="placements:PlacementDetail">

                                <!-- Button -->
                                <Button Style="{StaticResource BackstageListButton}" 
                                        Click="Placement_Click" Tag="{x:Bind Id}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontWeight="Bold" Text="{x:Bind EmployerName}"/>
                                        <StackPanel Grid.Column="0" Grid.Row="1" Spacing="2" Margin="0">
                                            <TextBlock Text="{x:Bind Position}" TextWrapping="WrapWholeWords"/>
                                            <TextBlock Text="{x:Bind FormattedHireDate}" FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" VerticalAlignment="Center"
                                                Style="{StaticResource AccentButtonStyle}"
                                                IsEnabled="{x:Bind Id, Converter={StaticResource NullToBoolConverter}}"
                                                Tag="{x:Bind Id}" Click="GoToPlacement_Click">
                                            <FontIcon Glyph="&#xE76C;"/>
                                        </Button>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsRepeater>
                    </ScrollViewer>
                </Grid>

                <!-- Checklist Card-->
                <Grid Style="{StaticResource CardContainerGridStyle}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Height="42" Style="{StaticResource CDOCardHeaderContainerStyle}">
                        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Checklists"
                                   Style="{StaticResource SubtitleTextBlockStyle}"/>
                    </Grid>


                    <!-- Inner Grid -->
                    <Grid Grid.Row="1" RowSpacing="4" Style="{StaticResource CDOCardContentContainerStyle}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>


                        
                        <!-- Resume Section -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Resume" FontWeight="Bold"/>
                        
                        <CheckBox Grid.Row="1" Grid.Column="0" Style="{StaticResource LeftCheckboxStyle}"
                                  Content="Required" Tag="{x:Bind dialogs:CheckboxTag.ResumeRequired}"
                                  IsChecked="{x:Bind _viewModel.Selected.ResumeRequired, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>
                        <CheckBox Grid.Row="1" Grid.Column="1" Style="{StaticResource RightCheckboxStyle}"
                                  Content="Completed" Tag="{x:Bind dialogs:CheckboxTag.ResumeCompleted}"
                                  IsChecked="{x:Bind _viewModel.Selected.ResumeCompleted, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>

                        <!-- Video Interviews Section -->
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Video Interviews"
                                   FontWeight="Bold"/>
                        <CheckBox Grid.Row="3" Grid.Column="0" Style="{StaticResource LeftCheckboxStyle}"
                                  Content="Required" Tag="{x:Bind dialogs:CheckboxTag.VideoInterviewRequired}"
                                  IsChecked="{x:Bind _viewModel.Selected.VideoInterviewRequired, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>
                        <CheckBox Grid.Row="3" Grid.Column="1" Style="{StaticResource RightCheckboxStyle}"
                                  Content="Completed" Tag="{x:Bind dialogs:CheckboxTag.VideoInterviewCompleted}"
                                  IsChecked="{x:Bind _viewModel.Selected.VideoInterviewCompleted, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>

                        <!-- Releases Section -->
                        <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"
                                   Text="Releases" FontWeight="Bold"/>
                        <CheckBox Grid.Row="4" Grid.Column="1" Style="{StaticResource RightCheckboxStyle}"
                                  Content="Completed" Tag="{x:Bind dialogs:CheckboxTag.ReleasesCompleted}"
                                  IsChecked="{x:Bind _viewModel.Selected.ReleasesCompleted, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>

                        <!-- Orientation Section -->
                        <TextBlock Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"
                                   Text="Orientation" FontWeight="Bold"/>
                        <CheckBox Grid.Row="5" Grid.Column="1" Style="{StaticResource RightCheckboxStyle}"
                                  Content="Completed" Tag="{x:Bind dialogs:CheckboxTag.OrientationCompleted}"
                                  IsChecked="{x:Bind _viewModel.Selected.OrientationCompleted, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>

                        <!-- 1850 Data Sheet Section -->
                        <TextBlock Grid.Row="6"  Grid.Column="0" VerticalAlignment="Center"
                                   Text="1850 Data Sheet" FontWeight="Bold"/>
                        <CheckBox Grid.Row="6"  Grid.Column="1" Style="{StaticResource RightCheckboxStyle}"
                                  Content="Completed" Tag="{x:Bind dialogs:CheckboxTag.DataSheetCompleted}"
                                  IsChecked="{x:Bind _viewModel.Selected.DataSheetCompleted, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>

                        <!-- Elevator Speech Section -->
                        <TextBlock Grid.Row="7" Grid.Column="0" VerticalAlignment="Center"
                                   Text="Elevator Speech" FontWeight="Bold"/>
                        <CheckBox Grid.Row="7" Grid.Column="1" Style="{StaticResource RightCheckboxStyle}"
                                  Content="Completed" Tag="{x:Bind dialogs:CheckboxTag.ElevatorSpeechCompleted}"
                                  IsChecked="{x:Bind _viewModel.Selected.ElevatorSpeechCompleted, Mode=OneWay}"
                                  Click="Checkbox_Clicked"/>
                    </Grid>
                </Grid>
            </controls:WrapPanel>
        </ScrollViewer>
    
        <!-- Row 2: Status Bar -->
        <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
              Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="8" 
                        VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Style="{StaticResource PageSubtitleStyle}" Margin="0">
                    <Run Text="ACE ID:"/>
                    <Run Text="{x:Bind _viewModel.Selected.Id, Mode=OneWay}"/>
                    <Run Text=","/>
                </TextBlock>
                <TextBlock Style="{StaticResource PageSubtitleStyle}" Margin="0">
                    <Run Text="Counselor ID:"/>
                    <Run Text="{x:Bind _viewModel.Selected.CounselorID, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Text="TTW" FontWeight="Bold" Style="{StaticResource PageSubtitleStyle}" Margin="0"
                           Visibility="{x:Bind _viewModel.Selected.TTW, Mode=OneWay, Converter={StaticResource BoolVisibilityConverter}}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="2" x:Name="InfoBarContainer" Margin="16" Width="400"
              VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <Grid.ChildrenTransitions>
                <TransitionCollection>
                    <AddDeleteThemeTransition/>
                </TransitionCollection>
            </Grid.ChildrenTransitions>
        </Grid>
    </Grid>
</Page>
