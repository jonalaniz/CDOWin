<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Backstage.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Backstage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Backstage.Views"
    mc:Ignorable="d"
    Title="Backstage">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <TitleBar Grid.Row="0" x:Name="Tbar" Height="48"
                  Title="Backstage" Subtitle="Beta"
                  IsBackButtonVisible="False" IsPaneToggleButtonVisible="True"
                  PaneToggleRequested="PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square44x44Logo.scale-200.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView Grid.Row="1" x:Name="NavigationView"
                        OpenPaneLength="200"
                        IsBackButtonVisible="Collapsed" 
                        IsPaneToggleButtonVisible="False"
                        SelectionChanged="nvSample_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home" Tag="{x:Bind views:BackstageView.Home}"/>
                <NavigationViewItem Content="Billing" Tag="{x:Bind views:BackstageView.Billing}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8C7;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <!--
                <NavigationViewItem Content="Clients" Tag="{x:Bind views:BackstageView.Clients}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE779;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                -->
                <NavigationViewItem Icon="People" Content="Users" Tag="{x:Bind views:BackstageView.Users}"/>
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
