<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Clients.Inspectors.Notes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:notes="using:CDO.Core.DTOs.Clients.Notes"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Notes"
                   Style="{StaticResource PageTitleStyle}"/>

        <!-- CommandBar -->
        <CommandBar Grid.Row="0" Grid.Column="1" DefaultLabelPosition="Collapsed" Background="Transparent">
            <!-- New Note -->
            <AppBarButton Width="56" Click="NewButton_Click" x:Name="NewButton" IsCompact="False" ToolTipService.ToolTip="New Client">
                <FontIcon Glyph="&#xE710;" Style="{StaticResource SingleIconFontStyle}"/>
            </AppBarButton>

            <!-- Export Menu -->
            <AppBarButton Width="56" IsCompact="False" ToolTipService.ToolTip="More">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE712;" Style="{StaticResource SingleIconFontStyle}"/>
                </AppBarButton.Icon>
                <AppBarButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedLeft">
                        <MenuFlyoutItem Text="Export Notes" Click="Export_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xEDE1;" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>
        
        <!-- Search Bar -->
        
        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                 Margin="10,0,10,8" PlaceholderText="Search Notes"
                 Text=""/>
        
        <!-- Notes ListView -->
        <ListView Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                  CornerRadius="0" BorderThickness="0" SelectionMode="None"
                  ItemsSource="{x:Bind ViewModel.Selected.ClientNotes, Mode=OneWay}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="notes:ClientNote">
                    <Button Margin="0" CornerRadius="0"
                            Background="Transparent" BorderThickness="0,1,0,0"
                            HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch"
                            Click="Note_Click" Tag="{x:Bind Id}">
                        <Grid Margin="0,4,0,4" RowSpacing="4" HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" FontWeight="Bold"
                                       Text="{x:Bind FormattedDate}"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" FontWeight="Bold"
                                       Text="{x:Bind Author}"
                                       Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,4,0,0"
                                       Text="{x:Bind Note}" TextWrapping="Wrap"
                                       IsReadOnly="True"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                        </Grid>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
