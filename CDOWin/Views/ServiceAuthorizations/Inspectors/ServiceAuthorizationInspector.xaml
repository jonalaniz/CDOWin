<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.ServiceAuthorizations.Inspectors.ServiceAuthorizationInspector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views.ServiceAuthorizations.Inspectors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:CDOWin.Controls"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Style="{StaticResource PageTitleStyle}"
                   Text="{x:Bind ViewModel.Selected.ClientName, Mode=OneWay}"
                   TextWrapping="WrapWholeWords"/>
        
        <!-- CommandBar -->
        <CommandBar Grid.Row="0" Grid.Column="1" DefaultLabelPosition="Collapsed" Background="Transparent">
            <AppBarButton Click="EditButton_Click" IsCompact="False" ToolTipService.ToolTip="Edit">
                <FontIcon Glyph="&#xE70F;" Style="{StaticResource SingleIconFontStyle}"/>
            </AppBarButton>

            <!-- More Menu -->
            <AppBarButton Width="56" IsCompact="False" ToolTipService.ToolTip="More">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE712;" Style="{StaticResource SingleIconFontStyle}"/>
                </AppBarButton.Icon>
                <AppBarButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedLeft">
                        <MenuFlyoutItem Text="Export..." Click="Export_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="OpenFile" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Delete..." Click="Delete_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Delete" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>

        <!-- Detials Grid -->
        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
              Style="{StaticResource InspectorDetialsGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0: PO Number + Case ID-->
            <control:LabelPair Grid.Row="0" Grid.Column="0" 
                               Label="SA Number" Value="{x:Bind ViewModel.Selected.ServiceAuthorizationNumber, Mode=OneWay}"/>
            <control:LabelPair Grid.Row="0" Grid.Column="1" 
                               Label="Case ID" Value="{x:Bind ViewModel.Selected.CaseId, Mode=OneWay}"/>

            <!-- Row 1: Counselor -->
            <control:LabelPair Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Issued By" Value="{x:Bind ViewModel.Selected.CounselorName, Mode=OneWay}"/>

            <!-- Row 2: Counselor -->
            <control:LabelPair Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Office" Value="{x:Bind ViewModel.Selected.Office, Mode=OneWay}"/>

            <!-- Row 3: Counselor -->
            <control:LabelPair Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Secretary" Value="{x:Bind ViewModel.Selected.SecretaryName, Mode=OneWay}"/>

            <!-- Row 4: Start Date + End Date -->
            <control:LabelPair Grid.Row="4" Grid.Column="0" Label="Start Date" Value="{x:Bind ViewModel.Selected.FormattedStartDate, Mode=OneWay}"/>
            <control:LabelPair Grid.Row="4" Grid.Column="1" Label="End Date" Value="{x:Bind ViewModel.Selected.FormattedEndDate, Mode=OneWay}"/>

            <!-- Row 5: Unit Cost -->
            <control:LabelPair Grid.Row="5" Grid.Column="0"
                               Label="Unit Cost" Value="{x:Bind ViewModel.Selected.FormattedCost, Mode=OneWay}"/>
            <control:LabelPair Grid.Row="5" Grid.Column="1"
                               Label="U/M" Value="{x:Bind ViewModel.Selected.UnitOfMeasurement, Mode=OneWay}"/>
            <!-- Row 6: Description -->
            <control:LabelPair Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Description" Value="{x:Bind ViewModel.Selected.Description, Mode=OneWay}"/>
        </Grid>
    </Grid>
</Page>
