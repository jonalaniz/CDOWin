<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Clients.Inspectors.Placements"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:inspectors="using:CDOWin.Views.Clients.Inspectors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CDOWin.ViewModels" 
    xmlns:models="using:CDO.Core.Models"
    d:DataContext="{d:DesignInstance Type=viewmodels:ClientsViewModel}"
    mc:Ignorable="d">

    <Grid Margin="16,8,16,0" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Placements -->
        <Grid Grid.Row="0" Grid.Column="0" RowSpacing="8" Style="{StaticResource BasicCardContainerGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="280"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Row 0: Title + Button-->
            <TextBlock Grid.Column="0" Text="Placements" Style="{StaticResource SubtitleTextBlockStyle}"/>

            <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}">
                <FontIcon Glyph="&#xE710;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}"/>
            </Button>

            <!-- Row 1: ListView -->
            <ListView Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                      Style="{StaticResource InspectorListViewStyle}"
                      ItemsSource="{x:Bind ViewModel.SelectedClient.referrals, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Referral">
                        <Grid RowSpacing="4" Margin="0,8,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock FontWeight="Bold" Text="{x:Bind employer.name, Mode=OneWay}"/>
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind formattedHireDate, Mode=OneWay}"/>
                                <TextBlock TextWrapping="WrapWholeWords" Text="{x:Bind position, Mode=OneWay}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        
        <!-- Service Authorizations -->
        <Grid Grid.Row="1" Grid.Column="0" RowSpacing="8" Style="{StaticResource BasicCardContainerGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="280"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0: Title + Button-->
            <TextBlock Grid.Column="0" Text="Service Authorizations"
                   Style="{StaticResource SubtitleTextBlockStyle}"/>

            <Button Grid.Column="1" Style="{StaticResource SubtleButtonStyle}">
                <FontIcon Glyph="&#xE710;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}"/>
            </Button>
            <ListView Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                      Style="{StaticResource InspectorListViewStyle}"
                      ItemsSource="{x:Bind ViewModel.SelectedClient.pos, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ServiceAuthorization">
                        <Grid RowSpacing="4" Margin="0,8,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="2">
                                <TextBlock FontWeight="Bold" Text="{x:Bind id, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind description, Mode=OneWay}"/>
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind formattedStartDate, Mode=OneWay}"/>
                                <TextBlock TextWrapping="WrapWholeWords" Text="{x:Bind formattedEndDate, Mode=OneWay}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
