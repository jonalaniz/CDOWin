<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.ClientsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:viewmodels="using:CDOWin.ViewModels"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:local1="using:CDO.Core.Models"
    xmlns:dto="using:CDO.Core.DTOs"
    d:DataContext="{d:DesignInstance Type=viewmodels:ClientsViewModel}"
    mc:Ignorable="d">

    <Grid>
        <!-- Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Column -->
        <Grid Grid.Column="0"
              BorderThickness="0, 0, 1, 0"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0"
                        Grid.Column="0">
                <TextBlock Text="Clients"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="16,8,16,1"/>
                <TextBlock FontSize="12"
                           Margin="16,1,16,8"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run Text="{x:Bind ViewModel.ClientSummaries.Count, Mode=OneWay}"/>
                    <Run Text="Clients"/>
                </TextBlock>
            </StackPanel>

            <Button Grid.Row="0"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Width="32"
                    Height="32"
                    Padding="0"
                    Margin="16,8,16,8"
                    Style="{StaticResource SubtleButtonStyle}">
                <FontIcon Glyph="&#xE710;"
                          FontSize="16"
                          Foreground="{ThemeResource SystemAccentColor}"/>
            </Button>

            <TextBox Grid.Row="1"
                     Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Margin="8, 0, 8, 8"
                     PlaceholderText="Filter by name"/>

            <ListView Grid.Row="2" 
                      Grid.Column="0"
                      Grid.ColumnSpan="2"
                      ItemsSource="{Binding ClientSummaries}"
                      IsItemClickEnabled="True"
                      ItemClick="ListView_ItemClick"
                      HorizontalAlignment="Left"
                      Width="220">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="dto:ClientSummaryDTO">
                        <Grid Margin="0,8,8,8">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0"
                                       Text="{x:Bind name}"
                                       FontWeight="Bold"
                                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                            <TextBlock Grid.Row="1" 
                                       Text="{x:Bind address1}"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>


        <!-- Middle Column -->
        <Frame Grid.Column="1"
               x:Name="ClientFrame" 
               IsNavigationStackEnabled="False"/>

        <!-- Right Column -->
        <Grid Grid.Column="2">
            <!-- Right Column Rows -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Right Column Colums -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <SelectorBar x:Name="SelectionBar"
                         SelectionChanged="SelectionChanged"
                         Grid.Column="0"
                         Grid.Row="0"
                         Margin="8, 0, 0, 8">
                <SelectorBarItem Text="Notes" IsSelected="True" />
                <SelectorBarItem Text="Documents" />
                <SelectorBarItem Text="Other" />
            </SelectorBar>
            
            <Frame Grid.Column="0"
                   Grid.Row="1"
                   x:Name="ContentFrame" 
                   IsNavigationStackEnabled="False"/>
        </Grid>
    </Grid>
</Page>
