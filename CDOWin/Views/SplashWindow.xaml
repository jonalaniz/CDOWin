<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    xmlns:winex="using:WinUIEx"
    x:Class="CDOWin.Views.SplashWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Loading...">

    <winex:WindowEx.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </winex:WindowEx.SystemBackdrop>

    <Grid>
        <!-- Bottom layer: accent color -->
        <Grid.Background>
            <SolidColorBrush Color="{ThemeResource SystemAccentColor}"/>
        </Grid.Background>

        <!-- Top: image as brush -->
        <Rectangle>
            <Rectangle.Fill>
                <ThemeResource ResourceKey="SplashBackgroundBrush"/>
            </Rectangle.Fill>
        </Rectangle>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="120"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="1" Margin="0,8,46,2"
                       Text="Version 1.1.0"
                       Foreground="GhostWhite"
                       Opacity="0.9"
                       HorizontalAlignment="Right"
                       FontWeight="SemiBold" 
                       FontSize="18"
                       x:Name="VersionBlock"/>
            <TextBlock Grid.Row="2" Margin="10,0"
                       Text="Loading..."/>
            <ProgressBar Grid.Row="3" VerticalAlignment="Bottom"
                         Foreground="{ThemeResource SystemAccentColorLight1}"
                         IsIndeterminate="True"/>
        </Grid>
    </Grid>
</winex:WindowEx>
