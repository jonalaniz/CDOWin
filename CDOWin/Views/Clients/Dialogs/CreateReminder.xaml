<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Clients.Dialogs.CreateReminder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views.Clients.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:CDOWin.ViewModels" 
    mc:Ignorable="d">

    <Grid Width="480" RowSpacing="12" ColumnSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Row 0: Date + Defers -->
        <CalendarDatePicker Grid.Row="0" Grid.Column="0" x:Name="DatePicker" 
                            Header="Date" HorizontalAlignment="Stretch"
                            DateChanged="DatePicker_DateChanged"/>
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Spacing="8">
            <TextBlock Text="Defer"/>
            <StackPanel Spacing="16" Orientation="Horizontal">
                <Button Content="1 Day" Click="Button_Click" Tag="1"/>
                <Button Content="1 Week" Click="Button_Click" Tag="7"/>
                <Button Content="45th Day" Click="Button_Click" Tag="44"/>
                <Button Content="90th Day" Click="Button_Click" Tag="89"/>
            </StackPanel>
        </StackPanel>

        <!-- Row 1: Description -->
        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4" Header="Description" 
                 TextChanged="TextChanged"
                 MaxLength="250"/>
    </Grid>
</Page>
