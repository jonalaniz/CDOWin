<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CDOWin.Views.CalendarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CDOWin.ViewModels"
    xmlns:models="using:CDO.Core.Models"
    mc:Ignorable="d"
    Title="Calendar">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid>
        <!-- Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title Bar -->
        <TitleBar Grid.Row="0" Grid.Column="0"
                  Title="CDO Win" Subtitle="Calendar"
                  IsBackButtonVisible="False"
                  IsPaneToggleButtonVisible="False">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square44x44Logo.scale-200.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <!-- Header Text -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="8,4">
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Row="0" Grid.Column="0" x:Name="MonthHeader" Margin="8,0,8,0" Style="{StaticResource PageTitleStyle}"/>
            
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="0">
                <!-- Export Button -->
                <Button Click="ExportButton_Click" Margin="0" 
                        Height="38" 
                        Foreground="{ThemeResource AccentAAFillColorDefaultBrush}"
                        Style="{StaticResource SubtleButtonStyle}" Content="Export">
                    <Button.Resources>
                        <ResourceDictionary>
                            <ResourceDictionary.ThemeDictionaries>
                                <ResourceDictionary x:Key="Light">
                                    <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{StaticResource AccentAAFillColorDefaultBrush}"/>
                                </ResourceDictionary>
                                <ResourceDictionary x:Key="Dark">
                                    <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="{StaticResource AccentAAFillColorDefaultBrush}"/>
                                </ResourceDictionary>
                            </ResourceDictionary.ThemeDictionaries>
                        </ResourceDictionary>
                    </Button.Resources>
                </Button>
                
                <!-- Decrement Button -->
                <Button Click="Button_Click" Tag="0" Margin="0" Style="{StaticResource SingleIconButtonStyle}">
                    <FontIcon Glyph="&#xE76B;" Style="{StaticResource SingleIconFontStyle}"/>
                </Button>

                <!-- Increment Button -->
                <Button Click="Button_Click" Tag="1" Margin="0" Style="{StaticResource SingleIconButtonStyle}">
                    <FontIcon Glyph="&#xE76C;" Style="{StaticResource SingleIconFontStyle}"/>
                </Button>
            </StackPanel>
        </Grid>
        
        
        <!-- Calendar Header -->
        <Grid Grid.Row="2" Grid.Column="0" Margin="12,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Sunday"/>
            <TextBlock Grid.Column="1" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Monday"/>
            <TextBlock Grid.Column="2" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Tuesday"/>
            <TextBlock Grid.Column="3" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Wednesday"/>
            <TextBlock Grid.Column="4" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Thursday"/>
            <TextBlock Grid.Column="5" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Friday"/>
            <TextBlock Grid.Column="6" Grid.Row="0" Margin="8,0" FontWeight="SemiBold" Text="Saturday"/>
        </Grid>

        <Border Grid.Row="3" CornerRadius="8" Margin="12,8,12,12" BorderThickness="1" 
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"    
                Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid x:Name="CalendarGrid">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
            </Grid>
        </Border>
    </Grid>
</Window>
