<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Clients.Dialogs.UpdateArrangements"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:CDOWin.Views.Clients.Dialogs"
    mc:Ignorable="d">

    <Grid Width="480" RowSpacing="10" ColumnSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Row 0: Employment Goals -->
        <TextBox Grid.Row="0" Grid.Column="0" Style="{StaticResource MultiLineTextBoxStyle}"
                 Header="Employment Goals" Text="{x:Bind ViewModel.OriginalClient.EmploymentGoal}"
                 Tag="{x:Bind local:ArrangementsField.EmploymentGoal}"
                 TextChanged="TextBox_TextChanged"/>

        <!-- Row 1: Conditions -->
        <TextBox Grid.Row="1" Grid.Column="0" Style="{StaticResource MultiLineTextBoxStyle}"
                 Header="Conditions" Text="{x:Bind ViewModel.OriginalClient.Conditions}"
                 Tag="{x:Bind local:ArrangementsField.Conditions}"
                 TextChanged="TextBox_TextChanged"/>
    </Grid>
</Page>
