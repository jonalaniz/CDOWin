<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Employers.Inspectors.EmployerInspector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views.Employers.Inspectors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:CDOWin.Controls"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.Column="0"
                   Style="{StaticResource PageTitleStyle}"
                   Text="{x:Bind ViewModel.Selected.Name, Mode=OneWay}"
                   TextWrapping="WrapWholeWords"/>

        <!-- CommandBar -->
        <CommandBar Grid.Row="0" Grid.Column="1" DefaultLabelPosition="Collapsed" Background="Transparent">
            <AppBarButton Click="EditButton_Click" IsCompact="False" ToolTipService.ToolTip="Edit">
                <FontIcon Glyph="&#xE70F;" Style="{StaticResource SingleIconFontStyle}"/>
            </AppBarButton>
        </CommandBar>

        <!-- Detials Grid -->
        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource InspectorDetialsGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0: Address -->
            <control:LabelPair Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Label="Address"
                               Value="{x:Bind ViewModel.Selected.FormattedAddress, Mode=OneWay}"/>

            <!-- Row 1: Phone + Fax -->
            <control:LabelPair Grid.Row="1" Grid.Column="0" Label="Phone" 
                               Value="{x:Bind ViewModel.Selected.Phone, Mode=OneWay}"/>
            <control:LabelPair Grid.Row="1" Grid.Column="1" Label="Fax" 
                               Value="{x:Bind ViewModel.Selected.Fax, Mode=OneWay}"/>

            <!-- Row 2: Supervisor -->
            <control:LabelPair Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Label="Supervisor" 
                               Value="{x:Bind ViewModel.Selected.FormattedSuperviror, Mode=OneWay}"/>
            <!-- Row 3: Website -->
            <control:LabelPair Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Label="Website" 
                               Value="{x:Bind ViewModel.Selected.Website, Mode=OneWay}"/>
            <!-- Row 4: Notes -->
            <control:LabelPair Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Label="Notes" 
                               Value="{x:Bind ViewModel.Selected.Notes, Mode=OneWay}"/>
        </Grid>
    </Grid>
</Page>
