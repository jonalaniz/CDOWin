<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.Clients.Inspectors.Notes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Notes"
                   Style="{StaticResource PageTitleStyle}"/>

        <!-- CommandBar -->
        <CommandBar Grid.Row="0" Grid.Column="1" DefaultLabelPosition="Collapsed" Background="Transparent">
            <!-- Save Note -->
            <AppBarButton Width="56" x:Name="SaveButton" IsCompact="False" Visibility="Collapsed"
                          Click="SaveButton_Click" ToolTipService.ToolTip="Save">
                <FontIcon Glyph="&#xE74E;" Style="{StaticResource SingleIconFontStyle}"/>
            </AppBarButton>

            <!-- New Note -->
            <AppBarButton Width="56" Click="NewButton_Click" x:Name="NewButton" IsCompact="False" ToolTipService.ToolTip="New Client">
                <FontIcon Glyph="&#xE710;" Style="{StaticResource SingleIconFontStyle}"/>
            </AppBarButton>

            <!-- Edit Menu -->
            <AppBarButton Width="56" IsCompact="False" ToolTipService.ToolTip="More">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE712;" Style="{StaticResource SingleIconFontStyle}"/>
                </AppBarButton.Icon>
                <AppBarButton.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedLeft">
                        <MenuFlyoutItem Text="Edit" Icon="Edit" Click="EditButton_Click"/>
                    </MenuFlyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>

        <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="12,0,12,12"
                 IsReadOnly="True" CornerRadius="4" VerticalAlignment="Stretch"
                 Style="{StaticResource MultiLineTextBoxStyle}"
                 TextWrapping="Wrap" MaxHeight="1500" x:Name="NotesBox"
                 Text="{x:Bind ViewModel.Selected.ClientNotes, Mode=OneWay}"/>
    </Grid>
</Page>
